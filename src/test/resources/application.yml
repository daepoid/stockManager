spring:

  #  # H2
  #  datasource:
  #    url: jdbc:h2:tcp://localhost/~/stockManager
  #    username: sa
  #    password:
  #    driver-class-name: org.h2.Driver

  #  # mariaDB
  #  datasource:
  #    driverClassName: org.mariadb.jdbc.Driver
  #    url: jdbc:mariadb://localhost:3306/stockManager?characterEncoding=UTF-8&serverTimezone=UTC
  #    username: root
  #    password: root

  # mysql
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/stockManager?allowPublicKeyRetrieval=true&useSSL=false
  #    url: jdbc:mysql://localhost:3306/stockManager?characterEncoding=UTF-8&serverTimezone=UTC&allowPublicKeyRetrieval=true&useSSL=false

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    #      ddl-auto: none
    properties:
      dialect:
        org.hibernate.dialect.MySQL5InnoDBDialect
      hibernate:
        #        show_sql: true
        format_sql: true
        default_batch_fetch_size: 100

#  handlebars:
#    suffix: .html
#    expose-session-attributes: true

logging:
  level:
    org.hibernate.SQL: debug
  #  org.hibernate.type: trace
#  pattern:
#    file: "[%d{HH:mm:ss.SSS}][%-5level][%logger.%method:line%line] - %msg%n"
#  file:
#    path: ../ChangeLogs
#  logback:
#    rollingpolicy:
#      max-file-size: 10MB
#      max-history: 400
#      file-name-pattern: "/logs/abc.%d{yyyy-MM-dd}.%i"

